--highlight each query to see a 10 record preview of each table

--preview distributors:
SELECT *
FROM distributors
LIMIT 10;

--preview rating:
SELECT *
FROM rating
LIMIT 10;

--preview revenue:
SELECT *
FROM revenue
LIMIT 10;

--preview specs:
SELECT *
FROM specs
LIMIT 10;